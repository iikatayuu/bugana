import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tk.bugana.bugana',
  appName: 'bugana',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
